document.addEventListener("DOMContentLoaded", function () {

    const dt = new DataTable('#tablaPersonasAnalizadas', {
        scrollX: true,
        autoWidth: false,
        paging: true,
        searching: true,
        ordering: true
    });

});
