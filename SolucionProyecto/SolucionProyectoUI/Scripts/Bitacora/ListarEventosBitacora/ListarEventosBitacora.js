document.addEventListener('DOMContentLoaded', function () {

    const dt = new DataTable('#tablaBitacora', {
        scrollX: true,
        autoWidth: false,
        paging: true,
        searching: true,
        ordering: true
    });
});
